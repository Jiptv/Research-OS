# Research OS Assets

Shared generic visual assets used across Research OS projects and rounds.

Do not put company-specific logos or deliverable artwork in this folder.

Use `../branding/` for local company assets such as `company-logo.png`. That
folder keeps real brand files out of Git, Docker build context and share
packages by default.
